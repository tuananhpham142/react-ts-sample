import { I18nextProvider, I18nextProviderProps } from 'react-i18next';

const I18nextProviderWrapper: React.FunctionComponent<I18nextProviderProps> = I18nextProvider;

export default I18nextProviderWrapper;
